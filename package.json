{
    "private": true,
    "scripts": {
        "start": "run-pty run-pty.json",
        "start:expo": "cd expo47 && yarn start",
        "build": "cd expo47 && yarn build",
        "test": "cd expo47 && yarn test"
    },
    "devDependencies": {
        "run-pty": "^5.0.0"
    }
}
